# Issue tracker contract

GitHub Issues in `Anionix/agent-work-governor` are the canonical task
specifications. The public **Agent Work Governor Roadmap** is only a view.

Only `ready-for-agent` work may start; promote blocked work after dependencies
merge. One branch and PR map to one Issue. Post-merge review fallout becomes a
linked `bug` + `review-fallout` Issue.
